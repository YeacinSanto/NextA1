## Generics

Generics is basically give you the independence to from declaring the type. Instead of fixed type you use a generic type (T) so that it can adapt based on what user passes

```
type GenericArray<T> = Array<T>
```
here you can use string array, number array and boolean array.

So generics also a good practice of DRY code